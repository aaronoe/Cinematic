<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="120dp"
    android:layout_height="160dp"
    android:layout_marginRight="8dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <ImageView
        tools:src="@drawable/test_actorimg"
        android:id="@+id/actor_item_iv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        />


    <ImageView
        android:src="@drawable/overlay_gradient_weak"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:textColor="@color/justWhite"
            android:textSize="11sp"
            android:textStyle="bold"
            tools:text="Tyrion Lannister"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/actor_name_tv"
            android:layout_marginBottom="2dp"
            android:layout_marginLeft="2dp"
            app:layout_constraintBottom_toTopOf="@+id/actor_character_name"
            app:layout_constraintLeft_toLeftOf="@+id/actor_character_name" />

        <TextView
            tools:text="Peter Dinklage"
            android:textColor="@color/grey_500"
            android:gravity="center"
            android:drawablePadding="2dp"
            android:drawableLeft="@drawable/ic_person_black_12dp"
            android:textSize="10sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:maxLines="2"
            android:layout_marginBottom="8dp"
            android:layout_marginLeft="6dp"
            android:id="@+id/actor_character_name" />

    </android.support.constraint.ConstraintLayout>


</FrameLayout>